import { PropsWithChildren } from 'react';
export declare const CheckBox: ({ className, active, children, _default, _onChange, reset }: PropsWithChildren<{
    className?: string;
    active: boolean;
    _default?: boolean;
    _onChange?: (checked: boolean) => void;
    reset?: boolean;
}>) => import("react").JSX.Element;
