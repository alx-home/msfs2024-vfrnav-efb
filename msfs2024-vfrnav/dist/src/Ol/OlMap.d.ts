import { MapContext } from '../MapPage/MapPage';
import { PropsWithChildren } from 'react';
export declare const OlMap: ({ children, id, className, mapContext }: PropsWithChildren<{
    id: string;
    className: string;
    mapContext: MapContext;
}>) => import("react").JSX.Element;
